# DO NOT RUN THESE DATABASE SCRIPTS

These files contain SQL queries that would alter or drop the live database schema (specifically the `profiles` table), resetting it with custom constraints that conflict with the actual live database structure.

**Do not run these against your live database.** They are kept here purely for reference.
